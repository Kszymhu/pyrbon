name: Build Docker Image
on:
  workflow_run:
    workflows: ["Pytest Tests"]
    types:
      - completed

jobs:
  build:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Download the repository
        uses: actions/checkout@v5
